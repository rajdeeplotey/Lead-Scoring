[project]
name = "lead-scoring"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = [
    "flask",
    "flask-cors",
    "gunicorn",
    "pandas",
    "numpy",
    "scikit-learn",
    "joblib",
    "python-dotenv"
]

[tool.vercel]
entrypoint = "web.backend.app:app"
